{
  "rules": {
    "blueprints": {
      ".read": "true",
      "$blueprint": {
        ".write": "!data.exists() || data.child('author').child('userId').val() === auth.uid",
        ".validate": "newData.hasChildren(['author'])",
        "author": {
          ".validate": "newData.child('userId').isString()",
          "userId": {
            ".validate": "newData.val() === auth.uid"
          }
        },
        "favorites": {
          "$uid": {
            ".write": "auth != null && auth.uid == $uid"
          }
        },
        "numberOfFavorites": {
          ".write": "auth != null"
        }
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid",
        "blueprints": {
          ".read": "true"
        },
        "displayName": {
          ".read": "true"
        }
      }
    }
  }
}
